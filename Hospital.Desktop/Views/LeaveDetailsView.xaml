<Window x:Class="Hospital.Desktop.Views.LeaveDetailsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="تفاصيل الإجازة" Height="480" Width="480" 
        WindowStartupLocation="CenterScreen" 
        FlowDirection="RightToLeft"
        Background="{StaticResource PrimaryBackground}"
        FontFamily="{StaticResource AppFontFamily}"
        ResizeMode="NoResize">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="تفاصيل الإجازة المسجلة" Style="{StaticResource Header1}"/>
            <TextBlock Text="معلومات الموظف والبديل وفترة الاجازة" FontSize="13" Foreground="{StaticResource TextMuted}" Margin="0,4,0,0"/>
            <Separator Margin="0,15,0,0" Background="{StaticResource BorderColor}" Height="1"/>
        </StackPanel>

        <Border Grid.Row="1" Background="{StaticResource SurfaceBackground}" CornerRadius="12" Padding="20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" ShadowDepth="1" Opacity="0.05" Color="Black"/>
            </Border.Effect>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="اسم الموظف المجاز" Foreground="{StaticResource TextMuted}" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding EmployeeName}" Grid.Column="1" Margin="20,8,0,8" FontSize="16" FontWeight="Bold" Foreground="{StaticResource PrimaryDark}"/>

                <TextBlock Text="الموظف البديل" Grid.Row="1" Foreground="{StaticResource TextMuted}" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding SubEmployeeName}" Grid.Row="1" Grid.Column="1" Margin="20,8,0,8" FontSize="14" Foreground="{StaticResource TextPrimary}"/>

                <TextBlock Text="فترة الإجازة" Grid.Row="2" Foreground="{StaticResource TextMuted}" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" Grid.Row="2" Grid.Column="1" Margin="20,8,0,8">
                    <TextBlock Text="{Binding StartDate, StringFormat=yyyy / MM / dd}" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}"/>
                    <TextBlock Text="←" Margin="10,0" Foreground="{StaticResource TextMuted}"/>
                    <TextBlock Text="{Binding EndDate, StringFormat=yyyy / MM / dd}" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}"/>
                </StackPanel>

                <TextBlock Text="المدة الإجمالية" Grid.Row="3" Foreground="{StaticResource TextMuted}" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center"/>
                <Border Grid.Row="3" Grid.Column="1" HorizontalAlignment="Left" Margin="20,8,0,8" Background="{StaticResource GovWarningSoft}" CornerRadius="6" Padding="12,5">
                    <TextBlock Text="{Binding Duration, StringFormat='{}{0} أيام'}" FontSize="14" FontWeight="Bold" Foreground="{StaticResource GovWarning}"/>
                </Border>
            </Grid>
        </Border>

        <Button Grid.Row="2" 
                Content="إغلاق" 
                IsCancel="True" 
                Width="120" 
                Height="42" 
                Margin="0,20,0,0" 
                Background="{StaticResource TextMuted}" 
                Foreground="{StaticResource SurfaceBackground}" 
                FontWeight="SemiBold"
                BorderThickness="0"
                Cursor="Hand">
            <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="8"/>
                </Style>
            </Button.Resources>
        </Button>
    </Grid>
</Window>