<Window x:Class="Hospital.Desktop.Views.DepartmentFormView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="بيانات القسم" 
        Height="480" Width="420" 
        WindowStartupLocation="CenterScreen" 
        FlowDirection="RightToLeft" 
        ResizeMode="NoResize"
        Background="{StaticResource PrimaryBackground}"
        FontFamily="{StaticResource AppFontFamily}">

    <Window.Resources>
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="Background" Value="{StaticResource SurfaceBackground}"/>
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="6"/>
                </Style>
            </Style.Resources>
        </Style>

        <Style x:Key="ActionBtn" TargetType="Button">
            <Setter Property="Padding" Value="20,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="6"/>
                </Style>
            </Style.Resources>
        </Style>

        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Height" Value="42"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="6"/>
                </Style>
            </Style.Resources>
        </Style>
    </Window.Resources>

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="اسم القسم" 
                       FontWeight="SemiBold" 
                       Foreground="{StaticResource TextMuted}" 
                       FontSize="13" 
                       Margin="0,0,0,8"/>
            <TextBox Text="{Binding DepartmentName, UpdateSourceTrigger=PropertyChanged}" 
                     Style="{StaticResource ModernTextBox}"
                     VerticalContentAlignment="Center"
                     Height="42"
                     BorderThickness="1.5">
                <TextBox.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="1" Opacity="0.03" Color="Black"/>
                </TextBox.Effect>
            </TextBox>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <TextBlock Text="مسؤول القسم " 
                       FontWeight="SemiBold" 
                       Foreground="{StaticResource TextMuted}" 
                       FontSize="13" 
                       Margin="0,0,0,8"/>
            <ComboBox ItemsSource="{Binding Employees}" 
          SelectedValuePath="Id" 
          DisplayMemberPath="Name" 
          SelectedValue="{Binding ManagerId}" 
          Text="{Binding ManagerSearchText, UpdateSourceTrigger=PropertyChanged}"
          IsDropDownOpen="{Binding IsManagerDropDownOpen}"
          IsEditable="True"
          IsTextSearchEnabled="False"
          StaysOpenOnEdit="True"
          Height="38"/>
        </StackPanel>

        <UniformGrid Grid.Row="2" Columns="2">
            <StackPanel Margin="0,0,10,0">
                <TextBlock Text="رقم الأمر الإداري" 
                           FontWeight="SemiBold" 
                           Foreground="{StaticResource TextMuted}" 
                           FontSize="13" 
                           Margin="0,0,0,8"/>
                <TextBox Text="{Binding ManagerOrderNumber}" 
                         Style="{StaticResource ModernTextBox}"
                         Height="42" BorderThickness="1.5"/>
            </StackPanel>

            <StackPanel Margin="10,0,0,0">
                <TextBlock Text="تاريخ التكليف" 
                           FontWeight="SemiBold" 
                           Foreground="{StaticResource TextMuted}" 
                           FontSize="13" 
                           Margin="0,0,0,8"/>
                <DatePicker SelectedDate="{Binding ManagerStartDate}" 
                            Height="42" 
                            VerticalContentAlignment="Center"/>
            </StackPanel>
        </UniformGrid>

        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Left" 
                    VerticalAlignment="Bottom">

            <Button Content="حفظ البيانات" 
                    Command="{Binding SaveCommand}" 
                    Style="{StaticResource PrimaryButton}" 
                    Width="130"
                    Height="42"/>

            <Button Content="إلغاء" 
                    IsCancel="True" 
                    Style="{StaticResource SecondaryButton}" 
                    Margin="12,0,0,0" 
                    Width="100"
                    Height="42"/>
        </StackPanel>
    </Grid>
</Window>